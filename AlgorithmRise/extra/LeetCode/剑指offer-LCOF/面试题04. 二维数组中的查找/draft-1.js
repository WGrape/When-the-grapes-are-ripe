/**
 * @param {number[][]} matrix
 * @param {number} target
 * @return {boolean}
 */
var findNumberIn2DArray = function(matrix, target) {

    // 思想: 优化减少遍历次数(二分 + 右斜对角线遍历)
    
    // 因为m*n所以需要额外的填补操作
    let width = matrix[0].length;
    let height = matrix.length;
    if(width < height){

    }else if(width > height){

    }


};