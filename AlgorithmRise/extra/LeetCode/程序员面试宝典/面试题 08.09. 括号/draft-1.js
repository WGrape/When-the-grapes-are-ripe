/**
 * @param {number} n
 * @return {string[]}
 */
var generateParenthesis = function(n) {
    
    // 1. ()
    // 2. ()(), (())
    // 3. ()()(), ((())),  

    // 左侧加, 内部加(外部加), 右侧加, 
    // "(" + x + ")" 直接用这种方式
};