/**
 * Definition for a binary tree node.
 * type TreeNode struct {
 *     Val int
 *     Left *TreeNode
 *     Right *TreeNode
 * }
 */
 func sortedArrayToBST(nums []int) *TreeNode {

    
    var level = 0, start = 0, end = math.Pow(2, level), len = len(nums) int

    for end <= len-1 {


    }
}