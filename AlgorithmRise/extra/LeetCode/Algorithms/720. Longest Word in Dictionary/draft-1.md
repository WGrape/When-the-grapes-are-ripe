```go

package longestWord

func longestWord(words []string) string {

    var hash = make(map[string]int)
    for _, value := range words{

        if val, err := hash{
            hash[value] = 0
        }
        hash[value]++
        fmt.Println(hash)
    }

    return ""
}
```