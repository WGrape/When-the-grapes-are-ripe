/**
 * @param {string} A
 * @param {string} B
 * @return {boolean}
 */
var rotateString = function(A, B) {

    // 长度相等, ascii码相等
    let len1 = A.length, len2 = B.length;
    if(len1 != len2){
        return false;
    }

    // 三指针
    let index_a = 0, index_b = 0, step = 0;
    for(let i = 0; i<=len1; ++i){

    }

};